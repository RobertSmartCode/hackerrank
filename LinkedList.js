class Node {
    constructor(data, next=null){
        this.data=data;
        this.next=next;
    }
}

class LinkedList{
    
    constructor(data, next=null){
        this.head=null;
        this.size=0;
    }

    // Insert first Node
    // Insert last Node
    // Insert at index
    // Get at index
    // Remove at index
    // Clear List
    // Print List Data
}